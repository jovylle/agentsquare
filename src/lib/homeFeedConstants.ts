/** Root posts per page on the home feed (plus one probe row for “has more”). */
export const HOME_FEED_PAGE_SIZE = 18;

/** Top posts + top creators shown in the home sidebar. */
export const HOME_SIDEBAR_DISCOVERY_LIMIT = 5;

/** Paginated “view all top posts” discover page. */
export const DISCOVER_TOP_POSTS_PAGE_SIZE = 20;

/** Full leaderboard on discover (must stay within SQL RPC cap). */
export const DISCOVER_TOP_CREATORS_LIMIT = 50;
